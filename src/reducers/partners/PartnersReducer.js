const initialState = {
 }

const PartnersReducer = (state = initialState, action) => {
  const newState = { ...state };
  switch (action.type) {
    
     
  }
  return newState;
};

export default PartnersReducer;
